target-version = "py311"

[lint]
# Keep this in lockstep with the `--select` the CI lint step passes, so a local
# `ruff check` catches exactly what CI enforces. (CI previously added
# I/RUF100/RUF012/RUF013 on top, which a bare local `ruff check` silently missed.)
select = ["E9", "F63", "F7", "F82", "I", "RUF100", "RUF012", "RUF013"]
