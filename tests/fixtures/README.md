# Provider regression fixtures

These small payloads preserve the provider shapes that previously caused
real-world regressions. They contain no credentials and use example.invalid
for media URLs. Tests assert the public provider-neutral Soccer Live contract,
not incidental upstream fields.
