[pytest]
# Home Assistant's test helpers use async fixtures/tests; auto mode lets the
# real-HA test (tests/test_ha_config_flow.py) run without per-test markers.
# The lightweight stub tests are synchronous and unaffected.
asyncio_mode = auto
